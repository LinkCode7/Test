#include "pch.h"
#include "boost_property_tree.h"

#include <boost/property_tree/ptree.hpp>

#include <boost/property_tree/xml_parser.hpp>

#include <locale>
#include <codecvt>

using namespace std;
using namespace boost;

using namespace property_tree;


void boost_property_tree()
{
	//return;

	//property_tree::ptree tree;

	//std::locale utf8Locale(std::locale(), new std::codecvt_utf8<wchar_t>); // codecvt_utf8
	//boost::property_tree::read_xml(string(DESKTOP_FILE_XML), tree, 0, utf8Locale);
	//
	////read_xml(DESKTOP_FILE_XML, tree);

	//auto value = tree.get<int>("Window.test");
}